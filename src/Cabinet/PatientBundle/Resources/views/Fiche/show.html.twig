{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Fiche</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Annee</th>
                <td>{{ entity.annee|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('fiche') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('fiche_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
